# 第 4 章：从头开始实现 GPT 模型以生成文本

- [flops-analysis.ipynb](flops-analysis.ipynb) 分析了主要章节中实现的 GPT 模型的每秒浮点运算次数 (FLOPS)。
- `requirements-extra.txt` 包括需要安装的额外 Python 库（通过 `pip install -r requirements-extra.txt` 安装）。
